# publish-homebrew configuration
# All values below show defaults. Required fields must be set.

# Required - set by init wizard
formula: queuestack
github_repo: https://github.com/domzilla/queuestack

# Release branch (default: master)
# release_branch: master

# Tag prefix (default: v)
# tag_prefix: v

# Version source (auto-detected if not set)
# Options: cargo, npm, file, tag
# version_source: cargo|npm|file|tag

# Post-publish commands (optional)
# Shell commands executed sequentially after publish.
# Stops on first failure. No placeholder substitution.
post_publish_commands:
  - git push origin --tags
